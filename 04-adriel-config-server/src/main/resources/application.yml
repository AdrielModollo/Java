server:
  port: 8888
spring:
  application:
    name: adriel-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AdrielModollo/adriel-config-server
          #username: username
          #password: password
          default-label: main
          search-paths:
          - 'greeting-service*'